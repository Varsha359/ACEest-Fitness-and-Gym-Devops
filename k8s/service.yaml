apiVersion: v1
kind: Service
metadata:
  name: aceest-service
  labels:
    app: aceest
spec:
  selector:
    app: aceest
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
